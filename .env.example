# Optional environment variables for local model execution.
# Do not put real secrets in this file.

# Hugging Face token, only needed for gated/private models.
HF_TOKEN=

# Optional cache roots if you want to override the committed config locally.
HF_HOME=
TRANSFORMERS_CACHE=
